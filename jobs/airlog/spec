---
name: airlog

templates:
  airlog.env.erb: airlog.env
  start: bin/start
  stop: bin/stop

properties:
  airlog:
    site_title:
    footnote:
    blog_dsn:
    github_api:
      post_repository_endpoint:

packages:
- airlog
